{
  "name": "aws-sam-docker-compose-example",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "aws-sdk": "^2.799.0"
  },
  "devDependencies": {
    "axios": "^0.21.2",
    "jest": "^26.6.3"
  },
  "scripts": {
    "acceptance-test": "jest --verbose --testTimeout=60000 --env=node __tests__/acceptance/"
  }
}
